# Lightweight requirements for CI (linting and basic testing)
# Excludes heavy ML dependencies (PyTorch, MLflow, sentence-transformers)

# Core API framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic<2

# Database
SQLAlchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9

# Auth
python-jose[cryptography]==3.3.0
cryptography==41.0.7

# HTTP client
httpx==0.25.2

# Storage (lightweight, no MLflow)
minio==7.2.0
boto3==1.34.0

# Utilities
python-multipart==0.0.6
tenacity==8.2.3
loguru==0.7.2
pypdf==3.17.4

# OpenAI (lightweight, no heavy deps)
openai>=1.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0

# Note: Excluded for CI:
# - sentence-transformers (requires PyTorch ~900MB + CUDA ~2GB)
# - mlflow (heavy dependencies)
# - qdrant-client (can be mocked for tests)

