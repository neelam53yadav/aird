# primedata DAG/runtime deps
loguru==0.7.3
minio==7.2.20
pydantic-settings>=2.0,<3

# PDF parsing + advanced regex (your logs show both missing earlier)
pypdf>=4.0,<6
regex==2023.8.8

# Qdrant client (upgrade it too)
qdrant-client==1.16.2

# OpenAI embeddings (used when embedder_name=openai-ada-002 / text-embedding-3-*)
openai>=1.0.0,<2.0.0

# keep compatible with Airflow constraints ecosystem
urllib3<1.27
cffi<2
